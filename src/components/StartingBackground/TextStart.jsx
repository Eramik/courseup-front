import React from 'react'
import styles from './TextStart.module.scss';

const TextStart = (props) => {
    return (
        <h2 className={styles.TextStart}>
            
        </h2>
    );
}

export default TextStart